服务器编译后，在运行机上崩溃，内网正常问题已经查明：
1.编译服务器上，某些文件独占打开，导致编译的和真实的文件不符（复制）
-- 运行机启动失败（std），基本上是这类问题。
-- 解决方案：重编，或者kill相关进程重编。
2.vs运行库不一致
-- 编译期和运行期vs运行库不一致。比如win7正常，win10异常，多半是这种问题。
-- 解决方案：把编译机的vs运行库拷贝一份到运行机（行内通用做法）